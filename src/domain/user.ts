export class User {
    constructor(
      public id: string,
      public name: string,
      public password: string
    ) {}
  
    // Aqui podemos adicionar regras de negócio, como validações, métodos adicionais etc.
  }
  